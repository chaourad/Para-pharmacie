@{ 
    Layout = null; 
}

@{
    ViewData["Title"] = "List of zabi";
}

<div class="row">
     <div  class="col-md-8 offset-md-2">

         <table class="table">
             <thead>
                 <tr class="text-center" >
                     <th></th>
                     <th></th>
                     <th></th>
                     <th>Actions</th>
                 </tr>
             </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>

                          <td class="align-middle">
                                <img class="rounded-circle" src="" alt="" style="max-width: 100px" />
                           </td>
                          <td class="align-middle"> 
                           
                          </td>
                          <td  class="align-middle">  
                            
                          </td>
                          <td class="align-middle"> 
                              <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id=""><i class="bi bi-pencil-square"></i>Edit </a> |
                                <a class="btn btn-outline-info" asp-action="Details" asp-route-id=""><i class="bi bi-eye"></i>Details </a> |
                                <a class="btn btn-danger text-white" asp-action="Delete" asp-route-id=""><i class="bi bi-trash"></i>Delete </a>
                          </td>

                    </tr>
                }
            </tbody>
        </table>
     </div>
</div>
<div style="position: fixed; right:25px; button :25px;" class="text-white">
    <a asp-controller="Fournisseur" asp-action="Create" class="btn btn-success">
         <i class="bi bi-plus-circle"></i> Add New</a>

</div>